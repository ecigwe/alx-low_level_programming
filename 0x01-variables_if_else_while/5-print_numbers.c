#include  <stdio.h>

/*
 * main : returns digits
 * returns : always returns 0
 */

int main(void)
{
	int number = 0;

	for (number = 0; number < 10; number++)
	{
		printf("%d", number);
	}
	printf("\n");
	return (0);
}
